[global]
    run as user = netdata
    history = 3996
    memory mode = javascript
    bind socket to IP = 0.0.0.0

[web]
    allow connections from = localhost 10.* 172.* 192.168.*
    allow dashboard from = localhost 10.* 172.* 192.168.*
